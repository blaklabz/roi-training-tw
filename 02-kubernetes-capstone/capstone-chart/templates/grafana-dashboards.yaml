apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-pod-dashboard
  labels:
    grafana_dashboard: "1"
data:
  istio-dashboard.json: |
    {{- .Files.Get "dashboards/pod-dashboard.json" | nindent 4 }}
