apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-istio-dashboard
  labels:
    grafana_dashboard: "2"
data:
  istio-dashboard.json: |
    {{- .Files.Get "dashboards/istio-dashboard.json" | nindent 4 }}
