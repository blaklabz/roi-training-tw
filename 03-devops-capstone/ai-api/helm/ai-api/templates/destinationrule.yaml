apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: {{ include "ai-api.name" . }}-dr
spec:
  host: {{ include "ai-api.name" . }}
  subsets:
    - name: v1
      labels:
        version: v1
    - name: v2
      labels:
        version: v2
